# data

Esta carpeta se usará como capa de persistencia inicial del proyecto.

## Estructura sugerida

- `data/_schema` — esquemas Zod por colección
- `data/_backups` — copias de seguridad
- `data/*.json` — colecciones principales

## Regla base

Cada archivo JSON debe contener una colección con un formato claro y consistentes IDs.
